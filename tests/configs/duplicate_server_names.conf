# Arquivo: duplicate_server_names.conf

server {
    listen 8080;
    server_name example.com;

    location / {
        root /var/www/html;
    }
}

server {
    listen 8080;
    server_name example.com;

    location / {
        root /var/www/html;
    }
}
