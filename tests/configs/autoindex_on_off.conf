# Arquivo: autoindex_on_off.conf

server {
    listen 80;
    server_name localhost;

    location /autoindex_on {
        root /var/www/autoindex_on;
        autoindex on;
    }

    location /autoindex_off {
        root /var/www/autoindex_off;
        autoindex off;
    }
}
