# Arquivo: upload_enable.conf

server {
    listen 80;
    server_name localhost;

    location /upload {
        root /var/www/upload;
        upload_enable on;
        upload_path /var/www/uploads;
    }
}
